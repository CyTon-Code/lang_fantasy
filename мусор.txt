def mov(link):
    """
        Эта функция сохраняет в файл данные если он есть и возвращает none
        если файла нет функция возвращает true
        :param link:
        :return:
    """
    pass




# разделители также будут рассматриваться как объекты:
# text = text.replace(" ", " SPACE ")
# text = text.replace("\n", " END ")


